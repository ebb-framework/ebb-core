/**
 * The Pipeline represents a sequence of asynchronous or synchronous tasks.
 */
export default class Pipeline {
  /**
   * Initialize a new Pipeline.
   */
  constructor() {
    this.stages = [];
  }
}
